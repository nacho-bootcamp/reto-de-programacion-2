const num1 = parseInt(prompt("ingresa el numero"));
const num2 = parseInt(prompt("ingresa el numero"));
const num3 = parseInt(prompt("ingresa el numero"));

const promedio = (num1 + num2 + num3) / 3;
if (promedio >= 7) {
  alert("APROBADO DE SUERTE");
}
